The instrumentation is polymorphic. I decompressed two fresh instrumentation payloads (base64 → deflate-raw). They are 5701 vs 7100 bytes and structurally different from each other and from your saved srcdoc_i_function. So its math cannot be reimplemented in Node; it must actually execute. Your browser plan was the right call, and there's no Node shortcut.
the server ships a freshly-obfuscated program every challenge. That program runs canvas + WebGL + typeof process + 37×[native code] checks (it returns null under Node, and rejects swiftshader/llvmpipe headless GPUs), then computes 4 numbers and posts them back.